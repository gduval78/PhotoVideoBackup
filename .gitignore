# macOS
.DS_Store

# Xcode — user-specific state (never shared)
*.xcuserstate
xcuserdata/

# Xcode build output
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Live NAS integration test — local credentials, never committed
nas-test-config.json
*.nas-config.json
